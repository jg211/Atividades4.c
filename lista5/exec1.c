#include <stdio.h>

int main() {
 int i, codigo;
 int validos = 0, invalidos = 0;
 for(i = 1; i = 10; i++){
     scanf("%d", &codigo);
     if(codigo >= 1000 & codigo <= 9999){
     printf("Codigo válido");
      }
     else{
         printf("Codigo inválido");
 }
 }
    return 0;
}
