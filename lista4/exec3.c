#include <stdio.h>

int main() {
    int i, numero, soma = 0;
    for(i = 1; i <+5 ; i++){
        scanf("%d", &numero);
        if(numero > 0){
        soma = soma + numero;
        }
    }
printf("Soma: %d\n", Soma);
return 0;
}
